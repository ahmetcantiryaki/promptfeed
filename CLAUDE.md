# CLAUDE.md
- TypeScript strict mode
- `any` kullanma, `unknown` + narrowing kullan
- Component'leri /components/ui (primitive) ve /components/features altında topla
- Tailwind class'larını clsx/cn helper ile birleştir
- Her PR'da `npm run build` ve `npm run lint` geçmeli